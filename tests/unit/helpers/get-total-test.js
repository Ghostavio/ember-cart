import { getTotal } from 'ember-cart/helpers/get-total';
import { module, test } from 'qunit';

module('Unit | Helper | get total');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = getTotal([42]);
  assert.ok(result);
});
