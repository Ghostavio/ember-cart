import { booleanDisabled } from 'ember-cart/helpers/boolean-disabled';
import { module, test } from 'qunit';

module('Unit | Helper | boolean disabled');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = booleanDisabled([42]);
  assert.ok(result);
});
