import { toMoney } from 'ember-cart/helpers/to-money';
import { module, test } from 'qunit';

module('Unit | Helper | to money');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = toMoney([42]);
  assert.ok(result);
});
