import { truncateWords } from 'ember-cart/helpers/truncate-words';
import { module, test } from 'qunit';

module('Unit | Helper | truncate words');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = truncateWords([42]);
  assert.ok(result);
});
